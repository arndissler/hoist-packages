{
  "name": "hoist-dev-packages",
  "version": "0.0.0",
  "description": "Check multiple package.json files for dev dependencies that can be hoisted when used in a monorepo environment.",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest",
    "watch:tests": "jest --watch",
    "start": "ts-node ./src/index.ts",
    "build": "tsc --project"
  },
  "keywords": [
    "monorepo",
    "hoisting",
    "dependencies"
  ],
  "author": "Arnd Issler <email@arndissler.net>",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^23.3.1",
    "@types/node": "^10.9.4",
    "@types/shelljs": "^0.8.0",
    "async-file": "^2.0.2",
    "jest": "^23.5.0",
    "prettier": "^1.14.2",
    "ts-jest": "^23.1.4",
    "ts-node": "^7.0.1",
    "typescript": "^3.0.3"
  },
  "dependencies": {
    "shelljs": "^0.8.2"
  }
}
